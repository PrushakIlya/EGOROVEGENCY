<?php

$sql = "CREATE TABLE results (id INTEGER UNSIGNED AUTO_INCREMENT PRIMARY KEY, 
result VARCHAR(50) NOT NULL,user_id INTEGER UNSIGNED NOT NULL,
FOREIGN KEY (user_id) REFERENCES users(id),
date TIMESTAMP NOT NULL)";

return $sql;
